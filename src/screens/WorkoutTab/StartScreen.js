import PlaceholderScreen from '../PlaceholderScreen.js';

// Workout tab — start screen (routine folders, Free Flow, resume interrupted).
// Implemented in issues #3/#4.
export default function StartScreen() {
  return (
    <PlaceholderScreen
      title="Workout"
      subtitle="Start a Free Flow workout or pick a routine. (issue #3)"
    />
  );
}